# 贡献指南 Contributing Guide

感谢你参与本项目! 为了保持代码库的整洁和高效, 请遵循以下准则.

- [Git commit 规范](./docs/commit_specification.md)
- [PR 操作流程](https://www.google.com/search?q=PR%E6%93%8D%E4%BD%9C%E6%B5%81%E7%A8%8B)

## 🌿 分支规范

- 请从 `main` 分支切出你的特性分支后开发
- 禁止直接在 `prod` 或 `main` 分支提交代码
- `git push` 前更新本地 `main` 分支, 若发现特性分支落后于 `main` 分支, 酌情使用 `git rebase main`
- 建议本仓库使用 Fast-forward 规则进行合并

## 📝 提交信息规范

1. **强制**使用 [Conventional Commits](https://www.conventionalcommits.org/en/v1.0.0/) 格式, 详情见 [Git commit 规范](./docs/commit_specification.md):

   ```bash
   <type>(scope): <subject>
   ```

2. 每次提交信息**禁止相同**, 如果有这个需求, 在本地使用 `git commit --amend` 修正提交

## 🚀 合并要求

合并流程详情见 [PR 操作流程](https://www.google.com/search?q=PR%E6%93%8D%E4%BD%9C%E6%B5%81%E7%A8%8B)

1. **必须 Rebase**：**开发者**在提交 PR 前，请确保你的分支已 rebase 到最新的 `main`.
2. **必须 Squash**：如果一个 PR 中有多个提交, **合并者**请勾选 GitHub 的 "Squash commits", 保持主干历史线性.
3. **代码评审**：**开发者**提交 PR 时, 必须设置一名 Maintainer 及以上权限的 Reviewer, Reviewer 通过方可合并.
